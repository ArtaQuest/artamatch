node_modules/
dist/
*.local
.DS_Store
.vite/
tsconfig.tsbuildinfo

# Scratch files that review agents write into the repo while probing. Three have been swept into
# commits by `git add -A` — one of them broke CI with a hardcoded absolute path.
_*.mjs
_*.ts
*.tmp.mjs
*.tmp.ts
tests/_*
tools/_*
